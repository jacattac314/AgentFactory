# {{ name }}

> Generated by Hermes Agent Factory on {{ created_at }}

## What This Agent Does
{{ description }}

## Original Request
> {{ user_request }}

## Template
`{{ template_name }}` — version `{{ version }}`

## Allowed Tools
| Service | Permitted Operations |
|---------|---------------------|
{% for service, ops in allowed_tools.items() %}
| `{{ service }}` | {{ ops | join(', ') }} |
{% endfor %}

## Denied Tools
| Service | Denied Operations |
|---------|------------------|
{% for service, ops in denied_tools.items() %}
| `{{ service }}` | {{ ops | join(', ') }} |
{% endfor %}

## Safety Gates
The following operations require explicit human approval:
{% for gate in approval_required_for %}
- `{{ gate }}`
{% endfor %}

## How to Validate
```bash
hermes factory validate {{ slug }}
```

## How to Dry-Run
```bash
hermes factory dry-run {{ slug }}
```

## How to Enable
Only run this after reviewing the generated code and validating.
```bash
hermes factory enable {{ slug }}
```

## Generated Files
```
agents/generated/{{ slug }}/
├── agent.yaml                    # Agent metadata and config
├── prompt.md                     # System prompt
├── tools.yaml                    # Tool permissions
├── workflow.py                   # Executable workflow (dry-run safe)
├── README.md                     # This file
└── tests/
    └── test_agent_permissions.py # Automated safety tests
```

## WARNING
This agent is **disabled by default** (`enabled: false`).
Running `hermes factory enable {{ slug }}` will first run validation.
If validation fails, the agent will not be enabled.
